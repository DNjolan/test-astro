---
interface Props {
    title: string;
    description: string;
    url: string;
}

const { title, description, url } = Astro.props;
---

<section class="photo-games">
    <h2>{title}</h2>
    <p>{description}</p>
    <img src={url} alt={title} />
</section>
---
